#include <stdio.h>

/**
 * main -entry point
 * @argc: number og arguments
 * @argv: list of arguments
 * Return: Always 0 (success)
 */
int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", argv[0]);
	return (0);
}
