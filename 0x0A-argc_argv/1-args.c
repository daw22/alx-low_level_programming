#include <stdio.h>

/**
 * main -entry point
 * @argc: number of args
 * @argv: args list
 * Return: Always 0 (success)
 */
int main(int argc, char **argv)
{
	argv = argv;
	printf("%d\n", argc - 1);
	return (0);
}
